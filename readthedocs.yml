version: 2

# Build image (use latest supported by Read the Docs)
build:
  image: latest

# Use MkDocs as the documentation builder
mkdocs: true

python:
  version: 3.10
  install:
    # Read the docs will install the requirements listed below
    - requirements: docs/requirements.txt

formats:
  - htmlzip
  - pdf